import 'package:flutter/material.dart';

class AllGenre extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
        appBar: AppBar(
            title: Text("ini halaman genre")
        ),
        body: Center(
         
        )
    );
  }
}